<div id="popup">
	<div id="close">
		<text>&#10005;<text>
	</div>
	<a href="http://twitter.com/<%= "#{@truck.twitter_handle}"%>"> <%="#{@truck.twitter_handle}"%></a>:
	
	<% if @truck.tweets.last %>
		<p><%= @truck.tweets.last.body%> </p>
	<% else %>
		<p>we do not have any recent tweets for this truck!</p>
	<% end %>
</div>