describe("View", function() {
  it("should be defined", function() {
    expect(MapWidget.View).toBeDefined();
  });

  it("and so is a spec", function() {
    a = true;

    expect(a).toBe(true);
  });
});
